import BasemapSwitcherButton from './basemap-switcher-button';

const plugin = {
  pluginName: 'BasemapSwitcher',
  enabled: true,
  components:[{
    component: BasemapSwitcherButton,
    region: 'left-sidebar-top',
    sortOrder: 30
  }]
}

export default plugin;