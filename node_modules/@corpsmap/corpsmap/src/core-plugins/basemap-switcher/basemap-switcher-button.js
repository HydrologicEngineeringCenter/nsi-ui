import React from 'react';
import BasemapList from './basemap-switcher-list';
import { SidebarPanelOpenerButton } from '../../application-controls/index';

class BasemapSwitcherButton extends React.Component {
  render() {
    return (
      <SidebarPanelOpenerButton
        iconClass="mdi mdi-google-maps"
        label="Basemaps"
        title="Basemaps"
        panelComponent={BasemapList}
      />
    );
  }
}

export default BasemapSwitcherButton;