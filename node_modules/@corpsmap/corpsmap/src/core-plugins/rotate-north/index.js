import RotateNorthButton from './rotate-north-button';

const plugin = {
  pluginName: 'NorthButton',
  enabled: true,
  components:[{
    component: RotateNorthButton,
    region: 'right-sidebar-top',
    sortOrder: 20
  }]
}

export default plugin;