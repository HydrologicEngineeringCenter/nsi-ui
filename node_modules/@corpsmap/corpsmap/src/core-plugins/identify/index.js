import IdentifyButton from './identify-button';
import bundle from './bundle';

const plugin = {
  pluginName: 'IdentifyTools',
  enabled: true,
  bundle: bundle,
  components:[{
    component: IdentifyButton,
    region: 'basic-toolbar',
    sortOrder: 90
  }]
}

export default plugin;