import LoginButton from './login-button';

const plugin = {
  pluginName: 'LoginButton',
  enabled: true,
  components:[{
    component: LoginButton,
    region: 'right-sidebar-top',
    sortOrder: 10
  }]
}

export default plugin;