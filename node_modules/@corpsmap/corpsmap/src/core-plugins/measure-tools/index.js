import MeasureToolsButton from './measure-tools-button';
import bundle from './bundle';

const plugin = {
  pluginName: 'MeasureArea',
  enabled: true,
  bundle: bundle,
  components:[{
    component: MeasureToolsButton,
    region: 'basic-toolbar',
    sortOrder: 50
  }]
}

export default plugin;