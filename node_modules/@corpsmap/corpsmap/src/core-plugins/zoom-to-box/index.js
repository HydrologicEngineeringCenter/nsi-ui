import ZoomToBoxButton from './zoom-to-box-button';
import bundle from './bundle';

const plugin = {
  pluginName: 'ZoomtoExtent',
  enabled: true,
  bundle: bundle,
  components:[{
    component: ZoomToBoxButton,
    region: 'basic-toolbar',
    sortOrder: 20
  }]
}

export default plugin;