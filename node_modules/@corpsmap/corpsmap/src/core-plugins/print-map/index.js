import bundle from "./bundle";
import Button from "./button";

const plugin = {
  pluginName: "Print Map",
  enabled: true,
  bundle: bundle,
  components: [
    {
      component: Button,
      region: "basic-toolbar",
      sortOrder: 200
    }
  ]
};

export default plugin;
