import React from "react";
import { connect } from "redux-bundler-react";
import { BasicToolbarButton } from "../../application-controls";

const Button = ({ doPrintMap }) => {
  return (
    <BasicToolbarButton
      enabled={true}
      iconClass="mdi mdi-printer"
      title="Print Map"
      onDeactivate={() => {}}
      onActivate={doPrintMap}
    />
  );
};

export default connect("doPrintMap", Button);
