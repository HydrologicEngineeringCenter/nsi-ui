import DrawButton from './draw-button';
import createDrawBundle from './create-draw-bundle';
import drawLabelBundle from './draw-label-bundle';
import drawModifyBundle from './draw-modify-bundle';
import drawSaveBundle from './draw-save-bundle';

const plugin = {
  pluginName: 'Draw Tools',
  enabled: true,
  bundle: [
    createDrawBundle({
      name: 'drawPoints',
      type: 'Point'
    }),
    createDrawBundle({
      name: 'drawLines',
      type: 'LineString'
    }),
    createDrawBundle({
      name: 'drawPolygons',
      type: 'Polygon'
    }),
    drawLabelBundle,
    drawModifyBundle,
    drawSaveBundle
  ],
  components:[{
    component: DrawButton,
    region: 'basic-toolbar',
    sortOrder: 80
  }]
}

export default plugin;