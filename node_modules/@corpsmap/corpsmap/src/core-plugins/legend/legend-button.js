import React from 'react';
import { SidebarPanelOpenerButton } from '../../application-controls';
import LegendPanel from './legend-panel';

class LegendButton extends React.Component {
  render() {
    return (
      <SidebarPanelOpenerButton
        iconClass="mdi mdi-format-list-bulleted-type"
        label="Legend"
        title="Legend"
        panelComponent={LegendPanel}
      />
    );
  }
}

export default LegendButton;