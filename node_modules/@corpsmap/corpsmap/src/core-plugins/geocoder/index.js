import SearchBar from './search-bar';
import bundle from './bundle';

const plugin = {
  pluginName: 'Geocoder',
  enabled: true,
  bundle: bundle,
  components:[{
    component: SearchBar,
    region: 'basic-toolbar',
    sortOrder: 100
  }]
}

export default plugin;