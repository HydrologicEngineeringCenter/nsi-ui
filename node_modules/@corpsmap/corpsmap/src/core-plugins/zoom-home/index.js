import ZoomHomeButton from "./zoom-home-button";
import bundle from "./bundle";

const plugin = {
  pluginName: "ZoomToInitial",
  enabled: true,
  bundle: bundle,
  components: [
    {
      component: ZoomHomeButton,
      region: "left-sidebar-top",
      sortOrder: 10
    }
  ]
};

export default plugin;
