export default {
  name: 'themeSwitcher',
  getReducer(){
    const initialData = {
      themes:[1,2,3]
    }

    return (state = initialData, { type, payload }) => {
      return state;
    }
  },

  selectThemes: (state) => {
    return state.themeSwitcher.themes;
  }
}