import bundle from './bundle';
import ThemeSwitcherButton from './theme-switcher-button';

const plugin = {
  pluginName: 'Theme Switcher',
  enabled: true,
  components:[{
    component: ThemeSwitcherButton,
    region: 'left-sidebar-bottom',
    sortOrder: 30
  }],
  bundle: bundle
}

export default plugin;